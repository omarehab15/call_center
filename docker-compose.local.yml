# docker-compose.local.yml — Local web stack with optional self-hosted SIP
# Default: LiveKit server + Redis + LiveKit Agent + Frontend
# Optional profile "sip": LiveKit SIP service
# STT runs on a remote machine (via Tailscale), LLM + TTS via Groq cloud
#
# Usage:
#   docker compose -f docker-compose.local.yml --env-file .env.local up --build -d

services:
  redis:
    image: redis:7-alpine
    container_name: callcenter_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - local_network
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 64M

  livekit:
    image: livekit/livekit-server:latest
    container_name: callcenter_livekit
    restart: unless-stopped
    ports:
      - "7880:7880"
      - "7881:7881"
    command: --dev --bind "0.0.0.0" --redis-host redis:6379 --node-ip livekit
    depends_on:
      redis:
        condition: service_started
    networks:
      - local_network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  sip:
    image: livekit/sip:latest
    container_name: callcenter_sip
    restart: unless-stopped
    profiles:
      - sip
    stop_grace_period: 5s
    ports:
      - "${SIP_SIGNALING_PORT:-5060}:5060/udp"
      - "${SIP_SIGNALING_PORT:-5060}:5060/tcp"
      - "${SIP_RTP_PORT_START:-10000}-${SIP_RTP_PORT_END:-10031}:${SIP_RTP_PORT_START:-10000}-${SIP_RTP_PORT_END:-10031}/udp"
    environment:
      SIP_CONFIG_BODY: |
        api_key: '${LIVEKIT_API_KEY:-devkey}'
        api_secret: '${LIVEKIT_API_SECRET:-secret}'
        ws_url: 'ws://livekit:7880'
        redis:
          address: 'redis:6379'
        sip_port: 5060
        sip_hostname: '${SIP_PUBLIC_HOST:-}'
        rtp_port: ${SIP_RTP_PORT_START:-10000}-${SIP_RTP_PORT_END:-10031}
        use_external_ip: ${SIP_USE_EXTERNAL_IP:-false}
        media_use_external_ip: ${SIP_MEDIA_USE_EXTERNAL_IP:-true}
        symmetric_rtp: ${SIP_SYMMETRIC_RTP:-true}
        ignore_local_addr_in_sdp: ${SIP_IGNORE_LOCAL_ADDR_IN_SDP:-true}
        enable_jitter_buffer: ${SIP_ENABLE_JITTER_BUFFER:-true}
        logging:
          level: ${SIP_LOG_LEVEL:-info}
    networks:
      - local_network
    depends_on:
      livekit:
        condition: service_started
      redis:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  livekit_agent:
    build:
      context: ./livekit_agent
    container_name: callcenter_agent
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      - LIVEKIT_URL=ws://livekit:7880
      - LIVEKIT_HOST=ws://livekit:7880
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET:-secret}
      - LIVEKIT_AGENT_NAME=${LIVEKIT_AGENT_NAME:-}
      # STT — remote (via Tailscale)
      - STT_PROVIDER=${STT_PROVIDER:-whisper}
      - STT_BASE_URL=${STT_BASE_URL}
      - STT_MODEL=${STT_MODEL:-dev-ahmedhany/whisper-large-v3-arabic-ft-v3-ct2-int8}
      - STT_API_KEY=${STT_API_KEY:-no-key-needed}
      # LLM + TTS — Groq cloud
      - GROQ_LLM_MODEL=${GROQ_LLM_MODEL:-openai/gpt-oss-20b}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - TTS_VOICE=${TTS_VOICE:-fahad}
      # SIP mode — enables BVCTelephony noise cancellation automatically
      - SIP_ENABLED=${SIP_ENABLED:-false}
      # Agent-side input noise cancellation: off, quail_vf_l, ai_coustics, nc, bvc, bvc_telephony
      # Default is off for local setup — quail_vf_l and ai_coustics require LiveKit Cloud.
      # Set AGENT_NOISE_CANCELLATION=bvc_telephony in .env.local for SIP, or nc/bvc for WebRTC.
      - AGENT_NOISE_CANCELLATION=${AGENT_NOISE_CANCELLATION:-off}
      # Loudness normalization (EBU R128) — normalizes caller audio before STT
      - LOUDNORM_ENABLED=${LOUDNORM_ENABLED:-true}
      - LOUDNORM_TARGET_LUFS=${LOUDNORM_TARGET_LUFS:--18}
      - LOUDNORM_MAX_GAIN_DB=${LOUDNORM_MAX_GAIN_DB:-15}
      - LOUDNORM_BUFFER_FRAMES=${LOUDNORM_BUFFER_FRAMES:-40}
      # Call Logs
      - CALL_LOGS=/app/call_logs
      # Local call recordings (stereo OGG: left=caller, right=agent)
      - CALL_RECORDING_ENABLED=${CALL_RECORDING_ENABLED:-true}
      - CALL_RECORDINGS_DIR=/app/call_recordings
      # RAG
      - RAG_ENABLED=${RAG_ENABLED:-true}
      - RAG_CHROMA_PATH=${RAG_CHROMA_PATH:-/app/rag/chroma}
      # FIX: use call_center_knowledge_chroma to match rag.py _default_collection_name()
      # which appends _chroma when RAG_EMBEDDING_PROVIDER=chroma (the default).
      # If you change RAG_EMBEDDING_PROVIDER, update this name accordingly.
      - RAG_COLLECTION_NAME=${RAG_COLLECTION_NAME:-call_center_knowledge_chroma}
      - RAG_EMBEDDING_PROVIDER=${RAG_EMBEDDING_PROVIDER:-chroma}
      - RAG_EMBEDDING_MODEL=${RAG_EMBEDDING_MODEL:-}
      - RAG_EMBEDDING_API_KEY=${RAG_EMBEDDING_API_KEY:-}
      - RAG_EMBEDDING_BASE_URL=${RAG_EMBEDDING_BASE_URL:-}
      - RAG_EMBEDDING_DEVICE=${RAG_EMBEDDING_DEVICE:-}
      - RAG_TOP_K=${RAG_TOP_K:-2}
      - RAG_MAX_CONTEXT_CHARS=${RAG_MAX_CONTEXT_CHARS:-1000}
      - RAG_KNOWLEDGE_DIR=${RAG_KNOWLEDGE_DIR:-/app/knowledge_base}
    volumes:
      - ${RAG_CHROMA_PATH_HOST:-./rag/chroma}:${RAG_CHROMA_PATH:-/app/rag/chroma}
      - ${RAG_KNOWLEDGE_DIR_HOST:-./livekit_agent/knowledge_base}:${RAG_KNOWLEDGE_DIR:-/app/knowledge_base}
      - ${CALL_LOGS_HOST:-./livekit_agent/call_logs}:${CALL_LOGS:-/app/call_logs}
      - ${CALL_RECORDINGS_HOST:-./call_recordings}:/app/call_recordings
    depends_on:
      livekit:
        condition: service_started
    stop_grace_period: 5s
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8081/ > /dev/null"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - local_network
    deploy:
      resources:
        limits:
          memory: 1400M
        reservations:
          memory: 600M

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880
      - LIVEKIT_URL=ws://livekit:7880
      - LIVEKIT_API_KEY=devkey
      - LIVEKIT_API_SECRET=secret
      - LIVEKIT_AGENT_NAME=${LIVEKIT_AGENT_NAME:-}
      # Limit Node.js heap to avoid frontend eating all RAM
      - NODE_OPTIONS=--max-old-space-size=256
    stop_grace_period: 5s
    depends_on:
      livekit:
        condition: service_started
    networks:
      - local_network
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

volumes:
  redis-data:

networks:
  local_network:
    driver: bridge
