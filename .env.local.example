# .env.local.example — Copy to .env.local and fill in your Tailscale IP
# Used with: docker compose -f docker-compose.local.yml --env-file .env.local up
#
# Replace 100.x.x.x with your vast.ai machine's Tailscale IP

# LiveKit
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# Remote model endpoints (via Tailscale)

# STT — Whisper on remote machine (port 11435)
STT_PROVIDER=whisper
STT_BASE_URL=http://100.x.x.x:11435/v1
STT_MODEL=dev-ahmedhany/whisper-large-v3-arabic-ft-v3-ct2-int8
STT_API_KEY=no-key-needed

# LLM — llama.cpp / ALLaM-7B on remote machine (port 11436)
LLAMA_BASE_URL=http://100.x.x.x:11436/v1
LLAMA_MODEL=allam-7b

# TTS — Provider toggle: "groq" (cloud) or "lahgtna" (self-hosted GPU)
TTS_PROVIDER=groq
TTS_VOICE=fahad

# Groq API — used when TTS_PROVIDER=groq
GROQ_API_KEY=gsk_your_key_here

# lahgtna-chatterbox on remote machine (port 8880) — used when TTS_PROVIDER=lahgtna
LAHGTNA_BASE_URL=http://100.x.x.x:8880/v1